name: Database Migrations

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to run migrations against'
        required: true
        default: 'staging'
        type: choice
        options:
        - staging
        - production
      dry_run:
        description: 'Dry run (validate only)'
        required: false
        default: false
        type: boolean

env:
  AWS_REGION: "us-east-1"

jobs:
  run-migrations:
    name: Run Database Migrations
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || 'staging' }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ env.AWS_REGION }}
        
    - name: Update GitHub IPs for RDS Access
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        echo "Ensuring GitHub Actions can access RDS..."
        
        # Trigger the IP update workflow
        gh workflow run update-github-ips.yml --ref ${{ github.ref }}
        
        # Wait a moment for the workflow to start
        sleep 10
        
        echo "✅ GitHub IPs update workflow triggered"
        
    - name: Get database connection details
      run: |
        ENVIRONMENT="${{ github.event.inputs.environment || 'staging' }}"
        echo "Getting database connection details for $ENVIRONMENT environment..."
        
        if [ "$ENVIRONMENT" = "staging" ]; then
          # For staging, use hardcoded values from appsettings.Development.json
          DB_HOST="staging-guras-db.cypui6c4kp4c.us-east-1.rds.amazonaws.com"
          DB_PORT="5432"
          DB_USER="guras_admin"
          DB_PASS="(jMP*S)OrQ15OpEj"
          DB_NAME="guras_staging"
        else
          # For production, get from AWS Secrets Manager
          DB_SECRET=$(aws secretsmanager get-secret-value \
            --secret-id "guras/db-credentials" \
            --region ${{ env.AWS_REGION }} \
            --query 'SecretString' \
            --output text)
          
          DB_HOST=$(echo "$DB_SECRET" | jq -r '.ServerName')
          DB_PORT=$(echo "$DB_SECRET" | jq -r '.Port')
          DB_USER=$(echo "$DB_SECRET" | jq -r '.UserName')
          DB_PASS=$(echo "$DB_SECRET" | jq -r '.Password')
          DB_NAME=$(echo "$DB_SECRET" | jq -r '.DatabaseName')
        fi
        
        echo "Database connection details retrieved:"
        echo "Host: $DB_HOST"
        echo "Port: $DB_PORT"
        echo "Database: $DB_NAME"
        echo "User: $DB_USER"
        
        # Set environment variables for Flyway
        echo "DB_HOST=$DB_HOST" >> $GITHUB_ENV
        echo "DB_PORT=$DB_PORT" >> $GITHUB_ENV
        echo "DB_NAME=$DB_NAME" >> $GITHUB_ENV
        echo "DB_USER=$DB_USER" >> $GITHUB_ENV
        echo "DB_PASSWORD=$DB_PASS" >> $GITHUB_ENV
        
    - name: Setup Flyway
      run: |
        echo "Setting up Flyway..."
        
        # Download Flyway
        wget -qO- https://repo1.maven.org/maven2/org/flywaydb/flyway-commandline/9.22.3/flyway-commandline-9.22.3-linux-x64.tar.gz | tar -xz
        
        # Add Flyway to PATH for current step
        export PATH="$PWD/flyway-9.22.3:$PATH"
        
        # Verify Flyway installation
        flyway --version
        
        # Set environment variable for next steps
        echo "FLYWAY_PATH=$PWD/flyway-9.22.3" >> $GITHUB_ENV
        
    - name: Run database migrations
      run: |
        ENVIRONMENT="${{ github.event.inputs.environment || 'staging' }}"
        DRY_RUN="${{ github.event.inputs.dry_run || false }}"
        
        echo "Running database migrations for $ENVIRONMENT environment..."
        
        if [ "$DRY_RUN" = "true" ]; then
          echo "🔍 DRY RUN MODE - Validating migrations only"
          ${{ env.FLYWAY_PATH }}/flyway validate \
            -url="jdbc:postgresql://${{ env.DB_HOST }}:${{ env.DB_PORT }}/${{ env.DB_NAME }}" \
            -user="${{ env.DB_USER }}" \
            -password="${{ env.DB_PASSWORD }}" \
            -locations="filesystem:database/migrations"
        else
          echo "🚀 Running actual migrations..."
          ${{ env.FLYWAY_PATH }}/flyway migrate \
            -url="jdbc:postgresql://${{ env.DB_HOST }}:${{ env.DB_PORT }}/${{ env.DB_NAME }}" \
            -user="${{ env.DB_USER }}" \
            -password="${{ env.DB_PASSWORD }}" \
            -locations="filesystem:database/migrations"
        fi
        
        echo "✅ Database migrations completed successfully!"


